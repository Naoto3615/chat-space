.side-bar
  .side-bar__user-name 
    .edit-bar
      .name 
        = current_user.name
      .icons
        .left-icon 
          = link_to new_group_path do
            %i.far.fa-edit
        .right-icon 
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog')
  
  .side-bar__group-list 
    - current_user.groups.each do |group| 
      .group-list
        = link_to group_messages_path(group) do
          .group-list--name 
            = group.name
          .group-list--name__message 
            = group.show_last_message

    